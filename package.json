{
  "name": "flashpoint-companion",
  "version": "1.0.0",
  "description": "Bootstrap for local testing",
  "main": "index.js",
  "scripts": {
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Me",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lite-server": "^2.2.2"
  }
}
